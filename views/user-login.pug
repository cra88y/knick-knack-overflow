extends layout.pug
include utils.pug
block content
    +validationErrorSummary(errors)
    form(action="/users/login" method="post")
        +field("", "_csrf", csrfToken, "hidden")
        +field("Email Address", "email", email, id="email-input",placeholder="Enter your email")
        +field("Password", "password", id="password-input", placeholder="Enter your password")
        button(id="login-page-btn" class='form-group') les go mate

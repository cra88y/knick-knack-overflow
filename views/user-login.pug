extends layout.pug
include utils.pug
block content
    +validationErrorSummary(errors)
    form(action="/users/login" method="post" id="login-form")
        +field("", "_csrf", csrfToken, "hidden")
        +field("Email Address", "email", email)
        +field("Password", "password")
        button(id="login-page-btn") les go mate
